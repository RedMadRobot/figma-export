{% include "header.stencil" %}
import UIKit

public extension UIColor {{ "{" }}{% for color in colors %}
    static var {{ color.name }}: YDSColor { YDSColor(name: #function) }{% endfor %}
}

extension YDSColor {
    static let allSets: Set<YDSColor> = [{% for color in colors %}
        .{{ color.name }},{% endfor %}
    ]
}
