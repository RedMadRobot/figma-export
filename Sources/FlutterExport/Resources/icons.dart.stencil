{% include "header.stencil" %}
class {{ iconsClassName }} {{"{"}}{% for variation in variations %}
  final String {{ variation }};{% endfor %}

  const {{ iconsClassName }}({{"{"}}{% for variation in variations %}
    required this.{{ variation }},{% endfor %}
  {{"}"}});
{% for icon in icons %}
  static const {{ icon.name }} = {{ iconsClassName }}({% for variation in variations %}
    {{ variation }}: '{{ icon.variations[variation] }}',{% endfor %}
  );{% endfor %}
{{"}"}}
