{% include "header.stencil" %}
import '{{ baseAssetClassFilePath }}';

class {{ imagesClassName }} {{"{"}}
  const {{ imagesClassName }}();
{% for image in images %}
  final {{ image.name }} = const {{ baseAssetClass }}({% for variation in variations %}
    {{ variation }}: {% if image.variations[variation] %}'{{ image.variations[variation] }}'{% else %}null{% endif %},{% endfor %}
  );{% endfor %}
{{"}"}}
